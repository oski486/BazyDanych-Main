[project]
name = "drugi"
version = "1.0.0"
description = "Moje środowisko"
authors = [
    {name = "Oskar M"}
]
requires-python = ">=3.14"
dependencies = [
    "sphinx (>=9.1.0,<10.0.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
